#include <iostream>
#include <set>

using namespace std;

int main()
{
    string str;
    set<string> s;
    while (cin >> str)
    {
        s.insert(str);
    }
    int num;
    cin >> num;
    for (int i = 0; i < num; i++)
    {
        i++;
    }
    
}
